document.querySelector('#results').innerHTML = '<%= j render 'users/result.html' %>'

<%# this takes the results div on my portfolio page and inserts the results partial with javascript
so that it can be displayed while using ajax %>
